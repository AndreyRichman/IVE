<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mta.ive">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.mta.ive.pages.SignInActivity"></activity>
        <activity android:name="com.mta.ive.pages.AllTasksActivity" />
        <activity android:name="com.mta.ive.pages.HomeActivity" />
        <activity android:name="com.mta.ive.pages.NewTaskActivity" />
        <activity android:name="com.mta.ive.pages.LobbyActivity" />
        <activity android:name="com.mta.ive.pages.LoginActivity" />
        <activity android:name="com.mta.ive.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>